name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  quality:
    name: Build, tests, and E2E
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: specforge_test
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d specforge_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5433/specforge_test?schema=public
      TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5433/specforge_test?schema=public

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run db:generate

      - name: Apply test database migrations
        run: npm run db:deploy

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Run frontend component tests
        run: npm run test:components:frontend

      - name: Run backend integration preflight
        run: npm run test:integration:backend

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E smoke tests
        run: npm run test:e2e
